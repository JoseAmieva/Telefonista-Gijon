# Copiar a /opt/telefonista-gijon/.env y rellenar
NODE_ENV=production
PORT=4000

# Mínimo 16 caracteres en producción
JWT_SECRET=cambiar_por_cadena_larga_aleatoria

APP_USER=user1
APP_PASSWORD=user1

# Origen del navegador (misma URL pública que usan los operadores)
CLIENT_ORIGIN=https://TU_DOMINIO
